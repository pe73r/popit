<svg
  class="{{ class }}"
  {% if small %}
  width="13"
  height="20"
  {% else %}
  width="20"
  height="20"
  {% endif %}
  viewBox="0 0 20 20"
  fill="none"
  xmlns="http://www.w3.org/2000/svg">
  <path
    d="M10 0.5V20"
    {% if color %}
    stroke="{{ color }}"
    {% else %}
    stroke="#201A1C"
    {% endif %}
    stroke-width="2" />
  <path
    d="M19.5 10.5L0 10.5"
    {% if color %}
    stroke="{{ color }}"
    {% else %}
    stroke="#201A1C"
    {% endif %}
    stroke-width="2" />
</svg>