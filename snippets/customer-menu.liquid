<div class="relative flex items-center justify-center w-2 lg:w-10 cursor-pointer">
  <a
    href="{{ routes.account_url }}"
    data-logged="{% if customer %}true{% else %}false{% endif %}"
    class="w-5 h-5 header__icon header__icon--account link focus-inset{% if section.settings.menu != blank %} small-hide{% endif %}">
    {% render 'icon-account' %}
  </a>
</div>